# Run the following apt command
sudo apt update

# List available security patches or updates:
sudo apt list --upgradable

# Apply those security patches on a Debian/Ubuntu server
sudo apt upgrade

# Reboot the system if Linux kernel was updated or patched for security issues:
sudo reboot